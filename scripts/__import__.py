import sys

ROOT = "PROJECT_DIR" # replace with the full path to this project (CLAS)
path = ROOT + "/probe_lib"
path_datasets = ROOT + "/datasets"
path_directions = ROOT + "/directions"
path_coefficients = ROOT + "/coefficients"

sys.path.insert(0, ROOT)